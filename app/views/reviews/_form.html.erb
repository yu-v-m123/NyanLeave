<%= form_with(model: [user, review], url: reviews_path(@user)) do |f| %>
  <%= f.text_area :content, placeholder: "この人への感想を書くにゃ！", class: "review-form" %>
  <%= f.submit "送信", class: "review-btn" %>
<% end %>